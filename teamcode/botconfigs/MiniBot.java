/*package org.firstinspires.ftc.teamcode.botconfigs;

import com.arcrobotics.ftclib.drivebase.DifferentialDrive;
import com.arcrobotics.ftclib.hardware.motors.Motor;
import com.qualcomm.robotcore.hardware.HardwareMap;
import org.firstinspires.ftc.robotcore.external.Telemetry;

// simple driving bot with two motors
public class MiniBot {

    // debugging device
    public Telemetry tele;

    // parallel motor drive train
    public DifferentialDrive drive;

    // initialize devices
    public MiniBot(Telemetry tele, HardwareMap map) {

        // store debugging device
        this.tele = tele;

        // initialize drive train
        drive = new DifferentialDrive(new Motor(map, "left"), new Motor(map, "right"));
    }
}
*/